#include "monty.h"

/**
 * custom_stack - Changes the mode to stack (LIFO).
 * @stack_head: Pointer to the stack's head
 * @line_num: Line number
 *
 * Return: No return value
 */
void custom_stack(stack_t **stack_head, unsigned int line_num)
{
	(void)stack_head;
	(void)line_num;
	bus.lifi = 0;
}
